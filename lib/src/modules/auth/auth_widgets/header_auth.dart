import 'package:flutter/material.dart';

class HeaderAuthentication extends StatelessWidget {
  const HeaderAuthentication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('')],
    );
  }
}
